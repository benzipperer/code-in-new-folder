{
  "$schema": "https://raw.githubusercontent.com/vicinaehq/vicinae/refs/heads/main/extra/schemas/extension.json",
  "name": "code-in-new-folder",
  "title": "Code in New Folder",
  "description": "Create a folder in a configurable base directory and open in your preferred code editor",
  "categories": ["Productivity"],
  "license": "MIT",
  "author": "benzipperer",
  "contributors": [],
  "pastContributors": [],
  "icon": "extension_icon.png",
  "commands": [
    {
      "name": "code-in-new-folder",
      "title": "Code in New Folder",
      "subtitle": "Create and open a project folder",
      "description": "Creates a folder in your configured base directory and opens it in your preferred code editor",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "type": "textfield",
      "name": "basePath",
      "title": "Base Directory Path",
      "description": "The base directory where folders will be created. Leave empty to use home directory. Supports ~ for home, relative paths, or absolute paths like / or /projects",
      "placeholder": "~",
      "default": "",
      "required": false
    },
    {
      "type": "textfield",
      "name": "programName",
      "title": "Editor Program Name",
      "description": "The name of the program to open folders with (e.g., 'code' for VS Code, 'positron' for Positron)",
      "placeholder": "positron",
      "default": "positron",
      "required": true
    },
    {
      "type": "checkbox",
      "name": "addYearToPath",
      "title": "Add Year to Path",
      "label": "Add year subdirectory to base path",
      "description": "When enabled, creates folders in YEAR/MM-DD/TITLE structure. When disabled, uses MM-DD/TITLE or just TITLE structure.",
      "default": true,
      "required": false
    },
    {
      "type": "checkbox",
      "name": "addMonthDayToPath",
      "title": "Add Month-Day to Path",
      "label": "Add month-day subdirectory to base path",
      "description": "When enabled, creates folders with MM-DD subdirectory. When disabled with year enabled, uses YEAR/TITLE structure.",
      "default": true,
      "required": false
    },
    {
      "type": "checkbox",
      "name": "sanitizePathName",
      "title": "Sanitize Path Name",
      "label": "Convert title to snake_case",
      "description": "When enabled, converts the title to lowercase snake_case and removes special characters. When disabled, uses the title as-is.",
      "default": true,
      "required": false
    },
    {
      "type": "checkbox",
      "name": "truncatePathName",
      "title": "Truncate Path Name",
      "label": "Limit title length",
      "description": "When enabled, limits the title to 50 characters or 10 words. When disabled, uses the full title.",
      "default": true,
      "required": false
    }
  ],
  "scripts": {
    "build": "vici build",
    "dev": "vici develop",
    "format": "biome format --write src",
    "lint": "vici lint"
  },
  "dependencies": {
    "@vicinae/api": "^0.16.11"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@biomejs/biome": "2.3.2"
  }
}
